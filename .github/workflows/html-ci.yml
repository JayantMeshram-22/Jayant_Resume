name: HTML Resume CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check HTML file exists
        run: |
          if [ -f index.html ]; then
            echo "✅ Resume HTML found"
          else
            echo "❌ index.html not found"
            exit 1
          fi

      - name: CI completed
        run: echo "🎉 Resume CI pipeline ran successfully!"
